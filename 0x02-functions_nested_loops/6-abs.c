#include "main.h"
/**
 * _abs - computes absolute value of an integer
 * @n: hold the integer we want to compute
 * Return: integers
 */

int _abs(int n)
{

	if (n < 0)
		return (-n);
	else
		return (n);
}
