#include "main.h"
/**
 * _abs - computes absolute value of an integer
 * @int: hold the integer we want to compute
 * Return: integers
 */

int _abs(int)
{

	if (int < 0)
		return (-int);
	else
		return (int);
}
